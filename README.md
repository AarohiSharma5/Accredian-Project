# Accredian Enterprise — Partial Clone

This is a production-ready, responsive partial clone of the Accredian Enterprise landing page built with Next.js (App Router) and Tailwind CSS.

Commands

```
npm install
npm run dev
```

Notes

- Mobile-first, sticky navbar, smooth scroll, basic animations
- Clean component architecture in `components/`
